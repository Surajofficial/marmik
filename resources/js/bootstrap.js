window._ = require("lodash"); try { window.Popper = require("popper.js").default, window.$ = window.jQuery = require("jquery"), require("bootstrap") } catch (e) { } window.axios = require("axios"), window.axios.defaults.headers.common["X-Requested-With"] = "XMLHttpRequest"; let token = document.head.querySelector('meta[name="csrf-token"]'); token ? window.axios.defaults.headers.common["X-CSRF-TOKEN"] = token.content : console.error("CSRF token not found: https://laravel.com/docs/csrf#csrf-x-csrf-token"), window.axios.defaults.headers.common = { "X-Requested-With": "XMLHttpRequest", "X-CSRF-TOKEN": document.querySelector('meta[name="csrf-token"]').getAttribute("content") }; import t from "laravel-echo"; window.Pusher = require("pusher-js"), window.Echo = new t({ broadcaster: "pusher", key: process.env.MIX_PUSHER_APP_KEY, cluster: process.env.MIX_PUSHER_APP_CLUSTER, forceTLS: !0, authEndpoint: baseURL + "/broadcasting/auth", encrypted: !0 });